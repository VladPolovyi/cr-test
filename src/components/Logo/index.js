import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  height: 100%;
  display: flex;
  align-items: center;
  font-weight: 700;
  font-size: 1.2rem;
`;

const Logo = () => {
  return <Wrapper>CRTEST</Wrapper>;
};

export default Logo;
